# Git Команды для работы с Codex - Памятка


## 🔍 Проверка и обновление информации о ветках

 - Скачать информацию о всех новых ветках с GitHub
 git fetch origin

 - Посмотреть все ветки (локальные + удалённые)
 git branch -a

 - Посмотреть только удалённые ветки
 git branch -r

 - Посмотреть только локальные ветки
 git branch

 - Проверить текущую ветку и статус
 git status





## Переключение на ветку Codex (ПРАВИЛЬНО)

 - 1. Обновить информацию о ветках
 git fetch origin

 - 2. Посмотреть список веток
 git branch -a

 - 3. Переключиться на ветку Codex (`короткое имя!`)
 git checkout codex/название-ветки

 - 4. Обновить состояние ветки
 git pull


 ### Частые ошибки при переключении
  
 - ❌ НЕПРАВИЛЬНО - приводит к detached HEAD
 git checkout remotes/origin/codex/название-ветки

 - ПРАВИЛЬНО - создаёт локальную ветку
 git checkout codex/название-ветки
 `Признак ошибки: Видите (HEAD detached at ...) в выводе git branch`


 ### Исправление:
 git checkout main
 git checkout codex/название-ветки





## 🔄 Обновление текущей ветки

 - Если уже на ветке - просто обновить
 git pull

 - Принудительное обновление (перезапись локальных изменений)
 git fetch origin
 git reset --hard origin/codex/название-ветки
 📝 Просмотр изменений и истории

 - Последние 5 коммитов
 git log --oneline -5

 - Коммиты только текущей ветки (без main)
 git log --oneline main..HEAD

 - Какие файлы изменены по сравнению с main
 git diff main --name-only

 - Детальные изменения с main
 git diff main

 - Посмотреть изменения в конкретном файле
 git diff main -- bot.ps1

 - Последний коммит с деталями
 git show HEAD





## 🔀 Слияние ветки Codex в main

 - 1. Переключиться на main
 git checkout main

 - 2. Обновить main с GitHub
 git pull origin main

 - 3. Слить с squash (все коммиты → один)
 git merge --squash codex/название-ветки

 - 4. Создать коммит с понятным сообщением
 git commit -m "Описание изменений"

 - 5. Загрузить main на GitHub
 git push origin main

 - 6. Удалить локальную ветку codex
 git branch -D codex/название-ветки

 - 7. Удалить ветку codex на GitHub
 git push origin --delete codex/название-ветки

 - 8. Проверить результат
 git log --oneline -3
 git branch -a





## 🗑️ Удаление веток

 - Удалить локальную ветку (если смержена)
 git branch -d codex/название-ветки

 - Удалить локальную ветку (принудительно)
 git branch -D codex/название-ветки

 - Удалить удалённую ветку на GitHub
 git push origin --delete codex/название-ветки





## ⏮️ Откат к предыдущим коммитам

 - Посмотреть историю
 git log --oneline

 - Просмотр без удаления (безопасно)
 git checkout HEAD~1  # на 1 коммит назад
 git checkout HEAD~3  # на 3 коммита назад
 git checkout main    # вернуться обратно

 - Удалить последние коммиты (ВНИМАНИЕ: удаляет изменения!)
 git reset --hard HEAD~1  # удалить 1 коммит
 git reset --hard HEAD~3  # удалить 3 коммита

 - Откат к конкретному коммиту
 git reset --hard abc1234

 - Загрузить откат на GitHub (требуется --force)
 git push origin codex/название-ветки --force





## 💾 Работа с локальными изменениями

 - Посмотреть несохранённые изменения
 git status
 git diff

 - Добавить файлы в staging
 git add .                  # все файлы
 git add file1.ps1 file2.ps1  # конкретные файлы

 - Создать коммит
 git commit -m "Сообщение коммита"

 - Добавить изменения к последнему коммиту
 git add .
 git commit --amend --no-edit

 - Изменить сообщение последнего коммита
 git commit --amend -m "Новое сообщение"

 - Загрузить на GitHub (требуется --force после amend)
 git push origin название-ветки --force

 ### 🚨 Если есть несохранённые изменения при pull

 - Ошибка: "Your local changes would be overwritten by merge"

 - Вариант 1: Спрятать временно (сохранить)
 git stash
 git pull
 git stash pop  # вернуть изменения

 - Вариант 2: Закоммитить
 git add .
 git commit -m "Local changes"
 git pull

 - Вариант 3: Отменить (потерять изменения)
 git checkout -- .
 git pull

 - Вариант 4: Принудительно перезаписать
 git fetch origin
 git reset --hard origin/codex/название-ветки





## 🔧 Переименование коммита

 - Изменить сообщение последнего коммита
 git commit --amend -m "Новое сообщение"

 - Загрузить изменённый коммит на GitHub
 git push origin main --force





## 🌐 Переименование репозитория

 - 1. На GitHub: Settings → Repository name → Rename

 - 2. Локально обновить URL
 git remote set-url origin https://github.com/User/НОВОЕ-ИМЯ.git

 - 3. Проверить
 git remote -v
 git fetch origin





## 📋 Проверка состояния (диагностика)

 - Где я сейчас?
 git branch

 - Синхронизирован ли с GitHub?
 git status

 - Посмотреть remote URL
 git remote -v

 - Сравнить локальную и удалённую ветки
 git log HEAD..origin/codex/название-ветки --oneline

 - Посмотреть все удалённые ветки
 git ls-remote --heads origin





## 🎯 Типичный Workflow с Codex

 ### Сценарий 1: Тестирование изменений от Codex

 - 1. Обновить список веток
 git fetch origin

 - 2. Посмотреть новую ветку
 git branch -a

 - 3. Переключиться на ветку Codex
 git checkout codex/название-ветки

 - 4. Тестировать...

 - 5. Если ОК - слить в main
 git checkout main
 git pull origin main
 git merge --squash codex/название-ветки
 git commit -m "Описание изменений"
 git push origin main

 - 6. Удалить ветку
 git branch -D codex/название-ветки
 git push origin --delete codex/название-ветки


 ## Сценарий 2: Доработка кода после Codex

 - 1. Переключиться на ветку Codex
 git checkout codex/название-ветки

 - 2. Внести изменения
 - ... редактируете файлы ...

 - 3. Закоммитить
 git add .
 git commit -m "Finalize: improvements"

 - 4. (Опционально) Обновить PR
 git push origin codex/название-ветки

 - 5. Слить в main
 git checkout main
 git merge --squash codex/название-ветки
 git commit -m "Финальная версия"
 git push origin main

 - 6. Удалить ветку
 git branch -D codex/название-ветки
 git push origin --delete codex/название-ветки


 ## Сценарий 3: Откат неудачных изменений

 - 1. Переключиться на ветку Codex
 git checkout codex/название-ветки

 - 2. Посмотреть историю
 git log --oneline

 - 3. Откатиться к нужному коммиту
 git reset --hard abc1234

 - 4. Загрузить откат на GitHub
 git push origin codex/название-ветки --force

 - 5. Создать новую задачу в Codex для переделки





## ⚠️ Важные правила
 * Всегда git fetch origin перед работой - обновить информацию о ветках
 * 
 * Используйте короткие имена веток - codex/..., не remotes/origin/codex/...
 * 
 * git pull работает только на текущей ветке - проверяйте через git branch
 * 
 * --force требуется после amend или reset - перезаписывает историю
 * 
 * Squash merge рекомендуется - объединяет все коммиты в один чистый
 * 
 * Удаляйте ветки после мержа - поддерживайте репозиторий в порядке





## 🆘 Режим detached HEAD - как исправить
 
 - Признаки:
 * (HEAD detached at origin/codex/...)
 
 - Исправление:
 - Вернуться на ветку
 git checkout main
 
 - Или создать ветку из текущего состояния
 git switch -c новое-имя-ветки
