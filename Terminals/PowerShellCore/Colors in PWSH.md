# PowerShell Core хороший терминал, НО настройка цветов для него это что-то СТРАШНОЕ
  Наш терминал берет свои настройки на разных уровнях и как это не странно этих уровней аж 7-8.5, ниже таблица с описанием всех уровней

+----------+--------+------------------------------------- ---+-----------------------------------  ------------+-----------------------------------------------+-------------------------------------------- ---+-----------------------------------------------+---------------------------------------------------------------------------------+
| PRIORITY | LEVEL  | NAME                                    | WHERE LIVES                                     | CONTROLS                                      | WHEN APPLIES                                   | OVERRIDES / DEPENDS                           | NOTES                                                                         |
+----------+--------+-----------------------------------------+-------------------------------------  ----------+-----------------------------------------------+------------------------------------------ -----+-----------------------------------------------+---------------------------------------------------------------------------------+
|   1      |   1    | 🎨 Explicit color in command            | Script / code                                   | конкретный цвет вывода строки (FG/BG)          | во время вывода                               | перекрывает всё ниже для этой строки          | пример: Write-Host -ForegroundColor Blue                                    |
|   2      |   2    | 🧷 prompt                               | Profile / runtime                               | приглашение PS> (может ANSI/VT)               | при каждом отображении prompt                  | перекрывает внешний вид prompt                | зависит от ANSI поддержки хоста                                             |
|   3      |   3    | ⌨️ PSReadLine -Colors                   | Profile / runtime                               | подсветка ВВОДА команд                         | во время ввода                                | не влияет на фон окна                         | Set-PSReadLineOption -Colors ...                                            |
|   4      |   4    | 🌈 $PSStyle (PS 7.2+)                   | Profile / runtime                               | раскраска вывода объектов (ANSI/VT)            | во время вывода объектов                       | зависит от ANSI поддержки хоста               | для WT/VS Code обычно ок                                                   |
|   5      |   5    | 🧱 $host.PrivateData                    | Profile / runtime                               | error/warning/verbose/debug цвета              | при выводе потоков                             | использует палитру 16 цветов                  | в ConHost опирается на ColorTable 0..15                                    |
|   6      |   6    | 🧨 $host.UI.RawUI                       | Runtime (current session)                       | фон/текст хоста текущей сессии                 | после вызова RawUI                             | может ломать WT/VS Code                       | в профиле НЕ трогать                                                       |
|   7      |  6.5   | 🔥 Shortcut / Taskbar (Console Props)   | .lnk / taskbar pin (Explorer)                   | “запечённые” свойства консоли                  | при старте из ярлыка/пинна                     | может перекрыть 7/8                           | при создании/закреплении Windows снимет слепок с 8 уровня, 7 проигнорирует  |
|   8      |   7    | 🎯 Registry per-app (pwsh.exe)          | HKCU:\Console\<pwsh.exe>                        | палитра 16 цветов + ScreenColors (FG/BG)       | до старта сессии (ConHost)                     | перекрывает 8 для pwsh                        | для pwsh могут быть два ключа: C_... и C:_...                              |
|   9      |   8    | 🧱 Registry global fallback              | HKCU:\Console                                   | глобальная палитра + ScreenColors по умолчанию | если 7 отсутствует / fallback                  | база для ConHost                              | часто источник слепка для 6.5, если 7 нет                                 |
|  10      |  WT    | 🧩 Windows Terminal settings.json       | Windows Terminal settings.json                  | background/foreground + scheme/palette         | в WT до старта pwsh                            | реестр 7/8 не задаёт фон WT                   | профиль PowerShell влияет на 2–6                                           |
|  11      | VSCode | 🧪 VS Code Terminal settings            | VS Code settings.json                           | ANSI palette + terminal background/foreground  | в VS Code до старта pwsh                       | реестр 7/8 не управляет VS Code               | terminal.ansi*, terminal.integrated.*                                      |
|  12      |  CLI   | 🚫 -NoProfile (launch mode)             | pwsh argument                                   | отключает профиль (и его настройки уровней 2–6)| при запуске pwsh                               | “отрезает” профильные изменения               | для диагностики чистых 7/8/6.5                                             |
+----------+--------+----------------------------------------+-----------------------------------------------+-----------------------------------------------+-----------------------------------------------+-----------------------------------------------+---------------------------------------------------------------------------------+



# 🧰 PowerShell Core Terminals — единая логика цветов (ConHost / Windows Terminal / VS Code)

  🎯 Цель: одинаковый Solarized Light (или любая другая схема) на всех компьютерах и во всех местах,
  где запускается `pwsh.exe`, без “магии” панели задач и случайных перекрытий.

---



## 0) 🧭 Термины (чтобы не путаться)

  - 🟦 **PowerShell Core** = `pwsh.exe` (движок/шелл).
  - 🖥️ **Терминал/хост** = то, что рисует окно и интерпретирует цвета:
    - 🪟 **ConHost** (автономное консольное окно Windows)
    - 🧩 **Windows Terminal (WT)** (tabs, settings.json)
    - 🧪 **VS Code Terminal** (встроенный терминал VS Code)

  ⚠️ Один и тот же `pwsh.exe` в разных хостах может выглядеть по-разному, потому что правила цветов у хостов разные.

---



## 1) 🗺️ Уровни цветов — “карта местности”

  Наша модель уровней (по смыслу и по приоритету).

  ### 1.1 ⚡ Runtime (внутри PowerShell-сессии): уровни 1–6
  Эти уровни применяются **после старта** и могут менять раскраску “на лету”.

  1) 🎨 **Явный цвет в команде**
    - Пример: `Write-Host -ForegroundColor ...`
    - Самый высокий приоритет для конкретной строки вывода.

  2) 🧷 **`prompt`**
    - Рисует приглашение `PS>` (может использовать ANSI/VT).

  3) ⌨️ **PSReadLine**
    - `Set-PSReadLineOption -Colors ...`
    - Влияет на подсветку того, что ты ВВОДИШЬ.

  4) 🌈 **`$PSStyle` (PowerShell 7.2+)**
    - Раскраска вывода таблиц/листингов через ANSI/VT.

  5) 🧱 **`$host.PrivateData`**
    - Error/Warning/Verbose/Debug часто используют именованные цвета (ConsoleColor → палитра 16).

  6) 🧨 **`$host.UI.RawUI`**
    - Может менять фон/текст в текущей сессии.
    - ⚠️ В ConHost работает “как ожидаешь”, в WT/VS Code может давать сюрпризы.
    - Мы считаем RawUI опасным для “единого стандарта”.

---



## 2) 🧾 Профиль PowerShell (наш “уровень 0.5”)

  ### 2.1 🧩 Что такое профиль в нашей системе
  Профиль — это **механизм инициализации**, который **выполняет код**, настраивающий уровни 2–6.

  ✅ Наша практика: **один единый профиль**
  - 📍 `I:\_Documents\PowerShell\profile.ps1`

  ### 2.2 🚫 Важно про `-NoProfile`
  Команда запуска:
  - `pwsh -NoProfile`

  ✅ Это означает: **профиль НЕ будет применён вообще**.
  Никакие профили PowerShell не выполняются.

  Использование:
  - 🧪 Диагностика “чистого” поведения цветов без вмешательства профиля.
  - 🔎 Проверка 7/8/6.5 без “шумов”.

---



## 3) 🗝️ ConHost реестр: уровни 8 и 7 (фундамент)

  ### 3.1 🧱 Уровень 8 — “глобальная база”
  - 🧾 Реестр: `HKCU:\Console`
  - Это нижний слой (fallback) для ConHost-приложений.

  ### 3.2 🎯 Уровень 7 — “персональные настройки конкретного EXE”
  - 🧾 Реестр: `HKCU:\Console\<ключ_для_pwsh.exe>`
  - Уровень 7 переопределяет 8 **для конкретного приложения**.

  Содержит:
  - 🎨 `ColorTable00..15` (16 ANSI-цветов)
  - 🧷 `ScreenColors` (FG/BG по индексам 0–15)

  ⚠️ Нюанс Windows:
  для `pwsh.exe` могут существовать **два ключа**, отличающиеся записью пути (например `C_...` и `C:_...`).
  Если хочешь железобетон — настраивай оба.

---



## 4) 🔥 Уровень 6.5 — ярлык/панель задач (самый коварный)

  ### 4.1 📌 Что это
  Это Console Properties, “зашитые” в `.lnk` / закрепление панели задач.
  Это **не реестр** и **не PowerShell**.

  ### 4.2 🎯 Ключевой факт (подтверждён наблюдениями)
  При **создании/закреплении** ярлыка Windows может автоматически сделать “слепок” консольных свойств
  и записать их внутрь ярлыка:

  - ✅ если уровень **7** для `pwsh.exe` существует → ярлык может “снять” **7**
  - ✅ если уровня **7** нет → ярлык может “снять” **8**

  После этого ярлык способен **перекрывать 7/8** и жить своей жизнью.

  Именно поэтому возможна “магия”:
  - запуск `pwsh.exe` напрямую → ✅ светлая тема (берёт 7/8)
  - запуск с панели задач → ❌ чёрная тема (берёт то, что “запёклось” в 6.5)

  📌 Наша договорённость:
  - 🚫 уровень 6.5 **никогда не настраиваем руками**
  - 🧽 если он мешает — его **убираем/пересоздаём**, а не “подгоняем”
  - 🧊 “запретить Windows делать слепок” надёжно не получается; управляем процессом через порядок действий (см. ниже)

---



## 5) 🧠 Наша стратегия (единый стандарт)

  ### 5.1 🧱 База: уровни 8 и 7 делаем одинаковыми
  Причины:
  - 🧩 8 — фундамент (и часто источник для “слепка” в 6.5)
  - 🎯 7 — персональный слой для `pwsh.exe` (для ConHost)

  ✅ Правило:
  - 🔁 Настраиваем **8 и 7 на одну и ту же схему** (например Solarized Light),
  чтобы даже если ярлык снимет “не тот” слой — результат был одинаковым.

  ### 5.2 🧾 Профиль используем только для “умных” вещей
  Профиль влияет и на ConHost, и на WT, и на VS Code (везде, где запускается `pwsh.exe`),
  поэтому профиль должен быть предсказуемым.

---



## 6) ✅ Что делать в профиле (рекомендации)

  ### ✅ Делать (безопасно и полезно)
  1) ⌨️ **PSReadLine цвета ввода (уровень 3)**
    - Подсветка команд/параметров при вводе.
    - Это не ломает базовый фон окна.

  2) 🌈 **`$PSStyle` (уровень 4)**
    - Раскраска вывода (ls/таблицы) через ANSI/VT.
    - Хорошо переносится между WT и VS Code (потому что это ANSI).

  3) 🧷 **`prompt` (уровень 2)**
    - Аккуратная настройка приглашения.

  4) 🧰 Опционально: настройка модулей, алиасов, quality-of-life,
    но без вмешательства в “фон/текст окна”.

  ### ❌ Не делать (чтобы не было рассинхрона)
  1) 🧨 **НЕ трогать `$host.UI.RawUI.BackgroundColor/ForegroundColor` (уровень 6)**
    - Это главный генератор “в одном месте так, в другом иначе”.
    - Особенно неприятно при WT/VS Code.

  2) 🧽 Не делать “Clear-Host после перекраски” ради эффекта —
    это скрывает причину и делает диагностику сложнее.

  3) 🧷 Не пытаться профилем “исправить” панель задач —
    панель задач это 6.5, профилем её не победить.

    ✅ Важное уточнение:
    - **Технически профилем можно "замазать" эффект 6.5** (например, перекрасить текущую сессию через RawUI/ANSI).
    - Но это будет **костыль**, потому что 6.5 всё равно останется источником стартовых Console Properties,
      а профиль лишь "перекрашивает" уже запущенное окно.
    - **Наша практика:** проблему 6.5 решаем на уровнях **7/8** (и через правильное закрепление/очистку),
      а профиль используем только для логики/удобства (`PSReadLine`, `$PSStyle`, `prompt`, модули).
      Так меньше путаницы и меньше рассинхрона между ConHost / WT / VS Code.

---



## 7) 🧩 Windows Terminal (WT): отдельный мир

  - 🛠️ WT задаёт цвета в `settings.json` (colorScheme, foreground, background, palette).
  - 🧾 Реестр 7/8 для ConHost WT обычно не использует как источник фона/текста.
  - ✅ Но профиль PowerShell в WT выполняется (если не `-NoProfile`), поэтому
    PSReadLine и `$PSStyle` работают и там.

  ✅ Рекомендация:
  - 🧾 В WT держать ту же схему (Solarized Light) в `settings.json`,
  - 🧾 а в профиле — только runtime-раскраску (PSReadLine/$PSStyle), без RawUI.

---



## 8) 🧪 VS Code Terminal: тоже отдельный мир

  VS Code имеет собственные настройки:
  - 🧷 `terminal.integrated.defaultProfile.windows`
  - 🧷 `terminal.integrated.profiles.windows`
  - 🧷 `terminal.integrated.env.windows`
  - 🎨 `terminal.integrated.*` цвета (ansi palette, background, foreground)

  VS Code может переопределять `terminal.ansi*` — это нормально, но это уже **VS Code-уровень**,
  не ConHost и не реестр.

  ✅ Рекомендация:
  - 🎨 Если нужна идентичность с Solarized Light — задаём палитру VS Code явно.
  - 🧾 Профиль PowerShell при этом оставляем “чистым” (PSReadLine/$PSStyle ок, RawUI нет).

---



## 9) 🧭 Правильный порядок действий (чтобы не ловить “магии”)

  ### 9.1 🧾 Настройка реестра
  1) 🧱 Выставить уровень 8 (HKCU:\Console) в нужную схему
  2) 🎯 Выставить уровень 7 для `pwsh.exe` (оба возможных ключа) в ту же схему
  3) 🧹 Закрыть ВСЕ окна ConHost/PowerShell
  4) ✅ Проверить:
    - `Win+R → pwsh` (ConHost) должно быть корректно
    - `pwsh -NoProfile` для “чистой” проверки

  ### 9.2 📌 Работа с панелью задач
  Поскольку 6.5 может снять слепок из 7/8 при закреплении:

  - 🧱 Сначала делаем 8 и 7 одинаковыми
  - 📌 Потом закрепляем
  - 🧩 Если панель задач всё равно ведёт себя иначе — считаем, что ярлык “запёкся”: Удаляем ярлык (Открепить или Удалить смотря где ярлык) и создаем новый ярлык из самого объекта C:\Program Files\PowerShell\7\pwsh.exe

---



## 10) 🔎 Диагностика (быстро понять, кто виноват)

  ### 10.1 🧪 Проверка без профиля
  - `pwsh -NoProfile`
  Если цвета внезапно “исправились” — виноват профиль.

  ### 10.2 🧬 Проверка хоста по родителю процесса
  Если родитель `explorer.exe` — запуск через Explorer/ярлык/панель задач.
  Если родитель другой (например WT/Code) — запуск в соответствующем хосте.

---



## 11) 🚫 Главные запреты (коротко)

  🚫 Не настраивать Console Properties вручную в свойствах окна/ярлыка (6.5).  
  🚫 Не лечить панель задач правками профиля.  
  🚫 Не красить фон/текст через RawUI в профиле (уровень 6).  
  ✅ Делать одинаковыми 8 и 7.  
  ✅ Профиль использовать для PSReadLine/$PSStyle/prompt.

---





## 12) 🏁 Итоговый принцип (золотая формула)
  **Не использую Автономное окно PowerShell Core** - после всех попыток настройть цвета в автономном окне все таки нашел способы это сделать, но возникла проблема со шрифтами и иконками, а эта проблема практически не решаемая для Автономного окна - поэтому было принято решение не использовать Автономное окно вообще.
  **ConHost стабильность** = ПУСТОТА на уровнях **8 + 7** - да решил на этих уровнях просто ничего не иметь
  **WindowsTerminal/VS Code** = будет две точки для настройкиWT и VS Code настраиваются в своих конфигурациях по отдельности но настройки должны быть одинаковыми для единобразия терминалов, а профиль добавляет только “умную” раскраску (PSReadLine/$PSStyle), не фон окна.
   - Для простоты цветовые схемы используются в WT/VS Code одинаковые и вся смена цветов это замена названия цветовой схемы. Практика ...
   - Шрифты также единые прописываются для терминала в WT/VS Code, более подробно смотреть в "Terminals\PowerShellCore\Fonts in PWSH.md"
