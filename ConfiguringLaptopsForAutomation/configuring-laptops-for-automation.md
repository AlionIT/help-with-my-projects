🚀 Настройка Рабочего Окружения (Deployment Guide)
  Краткая справка по развертыванию PowerShell Core, Windows Terminal и шрифтов с настройками на новой машине для автоматизации.



📦 1. Установка ПО (Через Winget)
  Запустите Windows PowerShell (старый, синий) от имени Администратора и выполните команды по очереди:
  
  PowerShell
  # 1. Установка PowerShell Core (pwsh)
  `winget install --id Microsoft.PowerShell --source winget --accept-package-agreements --accept-source-agreements`
  
  # 2. Установка Windows Terminal (если нет)
  `winget install --id Microsoft.WindowsTerminal --source winget --accept-package-agreements --accept-source-agreements`
  
  # 3. Установка Git (обязательно для работы)
  `winget install --id Git.Git --source winget --accept-package-agreements`

>  Важно: После установки рекомендуется перезагрузить ПК чтобы обновились пути (PATH).





📁 2. Создаем папку для работы на ноутбуке для автоматизации
  ● На главном диске создаем папку все связанное с работой за исключение того что будет ставиться в папки по умолчанию будет находиться в этой папке.
  ● Копируем в эту папку все необходимое для работы с флэшки содержащей папку с рабочего ноутбука "📁W:\ConfiguringLaptopsForAutomation" - на ней будут находиться папки и файлы:
    📁Work
        📁VSCode/ - эта папка создается из последней версии VSCode-win32-x64 скачанной в виде архива и разархивированной в данную папку + Дополнительно в папку VSCode помещаю папки с настройками
            Code.exe
            data/
                user-data/
                    User/ в эту папку помещаю файлы из рабочей версии VsCode: keybindings.json, myIconTheme.json, settings.json
                extensions/
        📁Fonts - в папке находятся все шрифты из папки с рабочего ноутбука "W:\MyIdeas\AlionIT\repositories\GH-AlionIT\help-with-my-projects\Terminals\Fonts"
        📁Terminal - в папке находятся скрипты для настройки и проверки терминала PowerShell Core
            Delete-Levels7-8.ps1
            FindFonts.ps1
            Show-All-Profiles.ps1
            ShowLevels7-8.ps1
            Test-Icons-And-Fonts.ps1
        📁Config - в папке находятся все настройки 
            📁WindowsTerminal
                settings.json
            📁VSCode
                keybindings.json
                myIconTheme.json
                settings.json
            📁Git
                tokens.txt
        📁midjourney-bot - в папке находится репозитарий в котором находятся скрипты для автоматизации
        🚀configuring-laptops-for-automation.md - это наш файл - он является нашей инструкцией на ноутбуке для автоматизации





🅰️ 3. Установка Шрифтов (Nerd Fonts)
  ● Для корректного отображения иконок и лигатур в терминале заходим в папку на ноутбуке для автоматизации 📁Work/Fonts/ и устанавливаем все имеющееся в ней шрифты: правой кнопкой мыши щелкаем по шрифту + Установить для всех пользователей





⚙️ 4. Настраиваем ноутбук для автоматизации
  🔷 Windows Terminal
    ● Открыть Windows Terminal.
    ● Нажать Ctrl + , (Запятая) -> открыть файл settings.json (внизу слева).
    ● Коректируем settings.json из нашего файла WTsettings.json


  🔷 VS Code
    ● Открыть VSCode и все должно уже выглядеть нормально.
    ● Закрепить VSCode на Панели задач
    ● Создать Рабочую область VsCode в папке рабочей в нашем случае это будет C:\Work\midjourney-bot\


  🔷 Клонируем репозитарий для работы
    ● Запускаем терминал в VsCode это сделать проще всего из Рабочей области VsCode и в терминале вводим
        # Настройка Git (один раз)
        git config --global user.name "VibeImp"
        git config --global user.email "ваш_email@example.com"

        # Клонирование репозитория
        git clone https://github.com/VibeImp/midjourney-bot.git

        # Переход в папку проекта
        cd midjourney-bot

        # Проверка подключения - Проверьте, что репозиторий подключен правильно:
        git remote -v

        # Вы должны увидеть:
        origin  https://github.com/VibeImp/midjourney-bot.git (fetch)
        origin  https://github.com/VibeImp/midjourney-bot.git (push)





🎯 Поздравляю - у нас все готово к работе!
